{
  "schemaVersion": "toograph.tool/v1",
  "toolKey": "video_segmenter",
  "name": "Video Segmenter",
  "description": "把本地视频切成适合批处理或后续 LLM 分析节点使用的短片段。",
  "version": "0.1.0",
  "timeoutSeconds": 600,
  "permissions": [
    "subprocess",
    "file_read",
    "file_write"
  ],
  "runtime": {
    "type": "python",
    "entrypoint": "run.py",
    "timeoutSeconds": 600
  },
  "inputSchema": [
    {
      "key": "video",
      "name": "Video",
      "valueType": "video",
      "description": "需要切分的本地视频文件。"
    }
  ],
  "outputSchema": [
    {
      "key": "segments",
      "name": "Segments",
      "valueType": "json",
      "description": "生成的视频片段数组，每项包含 index、start_sec、end_sec、local_path 和 mime_type。"
    }
  ],
  "localized": {
    "zh-CN": {
      "name": "视频分段器",
      "description": "把本地视频切成适合批处理或后续 LLM 分析节点使用的短片段。"
    },
    "en-US": {
      "name": "Video Segmenter",
      "description": "Splits a local video into short batch-ready clips for downstream Batch or LLM analysis nodes."
    }
  }
}
