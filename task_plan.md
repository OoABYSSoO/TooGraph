# Task Plan: GraphiteUI 前端优化与编排器工作区收口

## Goal

在不破坏现有视觉气质和核心交互的前提下，继续把 GraphiteUI 前端收口成真正可持续使用的工作台，其中本轮重点是把 `/editor` 改成可恢复、可多标签切换的真实编排器工作区。

## Current Phase

Phase 6

## Phases

### Phase 1: 当前前端现状梳理与设计方向确认

- [x] 读取 `ui-ux-pro-max` 技能
- [x] 基于当前仓库页面结构评估可优化方向
- [x] 建立 `task_plan.md` / `findings.md` / `progress.md`
- [x] 确认第一轮要改的页面范围
- **Status:** complete

### Phase 2: 首页与工作台入口优化

- [x] 强化首页 hero 与产品入口
- [x] 强化工作台的信息层级与操作入口
- [x] 保持现有暖色、纸张感与工作台语义
- **Status:** complete

### Phase 3: 全局壳层与导航优化

- [x] 优化侧栏导航的上下文感
- [x] 强化品牌区与当前页面高亮
- [x] 统一 hover / focus / active 状态
- **Status:** complete

### Phase 4: 首页与工作台合并收口

- [x] 将 `/` 收口为唯一主入口
- [x] 将 `/workspace` 并入 `/`
- [x] 保持现有视觉气质，改为最小前厅 + 最近运行/最近图双栏
- **Status:** complete

### Phase 5: 编辑器细节优化

- [x] 首页改为最近运行 / 模板选择 / 最近图三列
- [x] `/editor` 入口页改为模板与图并列同权
- [x] 左侧边栏支持完全收起与展开
- **Status:** complete

### Phase 6: 编排器 Tab 工作区

- [x] `/editor` 改为真实工作区，而非目录页
- [x] 新增 tab 工作区模型与本地持久化
- [x] 为 `NodeSystemEditor` 暴露文档元信息和保存回调
- [x] 收口 `/editor` 路由到单一 catch-all 页面
- [ ] 验证关闭脏 tab、URL 同步和恢复逻辑
- **Status:** in_progress

## Key Questions

1. 第一轮前端优化是优先动首页/工作台，还是先动编辑器细节？
2. 哪些现有页面是“产品入口页”，哪些是“工作台页”，应该有明显不同的层级？
3. 在不改掉当前视觉气质的前提下，哪些微交互最值得补？

## Decisions Made

| Decision | Rationale |
|----------|-----------|
| 保留当前暖色、纸张感、工作台式气质 | 现有产品已有辨识度，不应推倒重做 |
| 技能建议只用于辅助，不直接照搬设计系统 | `ui-ux-pro-max` 更偏 SaaS/营销页，不适合直接覆盖编辑器 |
| 先建立文件式规划 | 后续前端优化是多阶段任务，适合用持久文件跟踪 |
| 第一轮先动首页、工作台与导航壳层 | 风险较低，也最能快速提升产品入口感 |
| `/` 作为唯一主入口，`/workspace` 并入 `/` | 开源工具既要照顾新人理解，也要兼顾老用户日常效率 |
| 首页主体收口为“最近运行 + 最近图”双栏 | 更贴近日常使用路径，减少说明页和工作台页重复 |
| 首页继续升级为三列等权入口 | 模板选择也应成为首页主入口，而不是隐藏到 `/editor` 之后 |
| `/editor` 改成模板与图并列同权的目录页 | 首页承接快速开始，`/editor` 承接完整模板与图目录 |
| 侧栏折叠采用“完全收起 + 展开按钮” | 不引入 rail 图标体系，更符合当前视觉语境 |
| `/editor` 最终应是 tab 工作区，不再保留目录页心智 | 用户编排图时需要停留在画布内，不应通过二级页面往返切换图 |
| editor tab 仅持久化工作区元信息，编辑器实例由同一工作区壳层持有 | 这样能保持现有编辑器交互不变，同时避免切换 tab 时丢失内存态 |

## Errors Encountered

| Error | Attempt | Resolution |
|-------|---------|------------|
| 仓库当前没有前端测试框架 | 1 | 使用 `npx tsc --noEmit`、`git diff --check`、`./scripts/start.sh`、`curl` 路由探测替代本轮自动化验证 |
| 浏览器内交互无法完全自动化验证 | 1 | 先用纯函数测试锁定 tab/workspace 规则，再通过本地启动和页面探测做回归 |

## Notes

- 每完成一个前端阶段，就更新状态
- 所有显著设计判断都写入 `findings.md`
- 所有测试与结果都写入 `progress.md`
