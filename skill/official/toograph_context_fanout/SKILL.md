# TooGraph Context Fanout

Read-only Buddy context assembly. This skill fans out across platform memory,
knowledge retrieval, current page context, and enabled capability candidates,
then returns one budgeted `context_brief` plus an auditable merge report.

It must not write files, memories, graph state, settings, templates, or runtime
configuration. All outputs are context only and do not grant permissions.
