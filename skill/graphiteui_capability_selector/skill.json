{
  "schemaVersion": "graphite.skill/v1",
  "skillKey": "graphiteui_capability_selector",
  "name": "GraphiteUI Capability Selector",
  "description": "根据用户需求从启用的 GraphiteUI 能力中选择一个最合适的能力，优先返回图模板，其次返回 Skill；没有合适能力时返回 none。",
  "llmInstruction": "你已绑定 GraphiteUI Capability Selector。把用户的原始需求作为 requirement 传入；origin 通常填 companion。技能会只从启用且对当前来源可选择的图模板和 Skill 中选一个能力，优先图模板。不要自己编造能力，也不要把返回结果改写成多个能力。",
  "version": "1.0.0",
  "capabilityPolicy": {
    "default": {
      "selectable": true,
      "requiresApproval": false
    },
    "origins": {
      "companion": {
        "selectable": true,
        "requiresApproval": false
      }
    }
  },
  "permissions": ["file_read"],
  "inputSchema": [
    {
      "key": "requirement",
      "name": "Requirement",
      "valueType": "text",
      "required": true,
      "description": "用户希望完成的原始需求。"
    },
    {
      "key": "origin",
      "name": "Origin",
      "valueType": "text",
      "required": false,
      "description": "能力选择来源，例如 companion；默认 companion。"
    }
  ],
  "outputSchema": [
    {
      "key": "capability",
      "name": "Capability",
      "valueType": "capability",
      "description": "单个能力对象，kind 为 subgraph、skill 或 none。"
    }
  ],
  "runtime": {
    "type": "python",
    "entrypoint": "run.py",
    "timeoutSeconds": 30
  }
}
