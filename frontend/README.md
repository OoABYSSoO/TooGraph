# Frontend Scaffold

`frontend/` 目录已预留给 GraphiteUI 的 Next.js 前端工程。

当前仓库阶段：

- 产品文档已完成
- 后端已建立最小骨架
- 前端目录已创建，但尚未初始化为完整 Next.js 项目

下一步建议：

1. 在该目录中初始化 `Next.js + TypeScript`
2. 接入 `Tailwind CSS`
3. 接入 `shadcn/ui`
4. 建立 `app/` 路由结构

